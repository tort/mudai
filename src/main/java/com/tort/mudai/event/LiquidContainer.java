package com.tort.mudai.event;

public class LiquidContainer {
    public enum State {
        EMPTY,
        LESS_THAN_QUARTER,
        MORE_THAN_QUARTER,
        LESS_THAN_HALF,
        HALF,
        MORE_THAN_HALF,
        FULL
    }
}
